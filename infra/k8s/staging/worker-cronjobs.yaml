apiVersion: batch/v1
kind: CronJob
metadata:
  name: governance-ops-plan
  labels:
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: nwfwa-staging
spec:
  schedule: "45 1 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 7
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: worker
            app.kubernetes.io/part-of: nwfwa-staging
        spec:
          restartPolicy: OnFailure
          containers:
            - name: worker
              image: ghcr.io/replace-me/nwfwa-worker:staging
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: nwfwa-staging-config
                - secretRef:
                    name: nwfwa-staging-secrets
              command:
                - worker
                - build-governance-ops-plan
                - --object-storage-uri
                - s3://nwfwa-staging-artifacts
                - --database-ref
                - postgres://postgres:5432/fwa
                - --customer-scope-id
                - staging-customer
                - --retention-policy-id
                - staging-retention-v1
                - --backup-restore-plan-id
                - staging-backup-restore-v1
                - --legal-hold-policy-id
                - staging-legal-hold-v1
                - --cron
                - "45 1 * * *"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: ai-evidence-execution-plan
  labels:
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: nwfwa-staging
spec:
  schedule: "*/20 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 7
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: worker
            app.kubernetes.io/part-of: nwfwa-staging
        spec:
          restartPolicy: OnFailure
          containers:
            - name: worker
              image: ghcr.io/replace-me/nwfwa-worker:staging
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: nwfwa-staging-config
                - secretRef:
                    name: nwfwa-staging-secrets
              command:
                - worker
                - build-ai-evidence-execution-plan
                - --api-url
                - http://api-server:8080
                - --object-storage-uri
                - s3://nwfwa-staging-artifacts
                - --vector-store-kind
                - pgvector
                - --vector-store-ref
                - postgres://evidence-vectors
                - --customer-scope-id
                - staging-customer
                - --cron
                - "*/20 * * * *"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: analytics-export-plan
  labels:
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: nwfwa-staging
spec:
  schedule: "15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 7
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: worker
            app.kubernetes.io/part-of: nwfwa-staging
        spec:
          restartPolicy: OnFailure
          containers:
            - name: worker
              image: ghcr.io/replace-me/nwfwa-worker:staging
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: nwfwa-staging-config
                - secretRef:
                    name: nwfwa-staging-secrets
              command:
                - worker
                - build-analytics-export-plan
                - --object-storage-uri
                - s3://nwfwa-staging-artifacts
                - --clickhouse-url
                - http://clickhouse:8123
                - --customer-scope-id
                - staging-customer
                - --cron
                - "15 * * * *"
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pilot-readiness-proof
  labels:
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: nwfwa-staging
spec:
  schedule: "*/30 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: worker
            app.kubernetes.io/part-of: nwfwa-staging
        spec:
          restartPolicy: OnFailure
          containers:
            - name: worker
              image: ghcr.io/replace-me/nwfwa-worker:staging
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: nwfwa-staging-config
                - secretRef:
                    name: nwfwa-staging-secrets
              command:
                - worker
                - check-pilot-readiness
                - --api-url
                - http://api-server:8080
                - --require-ready
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: mlops-monitoring-plan
  labels:
    app.kubernetes.io/name: worker
    app.kubernetes.io/part-of: nwfwa-staging
spec:
  schedule: "0 2 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 7
  failedJobsHistoryLimit: 7
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app.kubernetes.io/name: worker
            app.kubernetes.io/part-of: nwfwa-staging
        spec:
          restartPolicy: OnFailure
          containers:
            - name: worker
              image: ghcr.io/replace-me/nwfwa-worker:staging
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: nwfwa-staging-config
                - secretRef:
                    name: nwfwa-staging-secrets
              command:
                - worker
                - build-mlops-monitoring-plan
                - --manifest-uri
                - s3://nwfwa-staging-artifacts/datasets/public-mvp/manifest.json
                - --artifact-uri
                - s3://nwfwa-staging-artifacts/models/baseline_fwa/staging/rust_serving_artifact.json
                - --model-key
                - baseline_fwa
                - --model-version
                - staging
                - --cron
                - "0 2 * * *"
