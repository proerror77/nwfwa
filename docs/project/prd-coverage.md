# PRD Coverage

This document maps the product PRD to the current repository evidence. It is
intended to answer what is complete in code, what is only a staging proof, and
what still requires customer data or a customer environment.

## Summary

The repository has implemented the demo and pilot-contract surfaces that can be
completed without real customer data:

- inbound claim inbox, normalization, correction templates, and canonical trace;
- deterministic scoring, FWA rule pack, review modes, rule lifecycle, and
  promotion controls;
- customer-approved deterministic adjudication-policy boundary for hard-deny,
  straight-through, pending-evidence, manual-review, and score-only rules;
- lead, case, investigation, QA, medical review, label, and audit workflows;
- model registry, evaluation gates, retraining jobs, Rust artifact scoring,
  external training handoff, and scheduled MLOps monitoring-plan contracts;
- dataset, feature-set, public-data MVP, and label-governance contracts;
- knowledge search, assistive agent investigation, and AI evidence metadata;
- ClickHouse analytics-scale schema, dashboard queries, export contracts, and
  provider graph snapshots;
- Kubernetes staging manifests, container packaging, GitHub Environment staging
  package workflow, pilot-foundation proof artifacts, customer-gated production
  deployment package, production observability manifests, and production
  readiness evidence contract;
- Yew Operations Studio demo surface.

The remaining production boundary is not another local module. It is customer
validation and environment execution:

- real customer labels and label provenance;
- customer holdout validation and live shadow traffic;
- customer-approved production deployment, secrets, retention, observability,
  OCR/vector workers, analytics execution, and network controls;
- customer-executed live restore, rollback, alert, and operational drills beyond
  the staging `operational_drill_proof.json` and production readiness evidence
  contracts.

The June 12 architecture gap review adds an additional distinction: several
surfaces are implemented as demo, baseline, staging proof, or contract shape but
are not yet production-complete FWA capabilities. The dated gap ledger is
`docs/project/architecture-gap-review-2026-06-12.md`.

## Machine-Checkable Proof

Generate the local proof artifact:

```bash
python3 scripts/ops/build_prd_coverage.py --output-dir artifacts/prd-coverage
```

The command writes:

- `prd_coverage_summary.json`
- `index.json`

CI runs the same script in the `staging-proof` job. The script fails when an
expected code, document, workflow, or proof artifact is missing.
It now treats worker data pipeline productionization as its own
customer-validation-boundary capability, covering governed worker commands,
customer-data write paths, readiness gates, run-status templates, and execution
evidence. The proof also checks that persisted scoring-feature-context
materializations are consumed by `/api/v1/claims/score` when no inline context
is supplied, and that persisted provider profile window rollups can populate
claim-time provider profile scoring inputs. Complete persisted provider graph
signals can also populate claim-time provider relationship scoring inputs.
The worker can fetch configured OIG/SAM-compatible snapshot endpoints into the
same governed sanctions sync artifact shape, and persisted OIG/SAM sanctions can
populate claim-time provider sanctions scoring inputs even when the request has
no inline `provider_profile`. Persisted
clinical compatibility references can populate claim-time clinical compatibility
scoring inputs when no inline or materialized scoring context is supplied.
Persisted episode rollups can populate claim-time episode-utilization scoring
inputs under the same fallback rule. Persisted unbundling comparator candidates
can populate claim-time `unbundling_candidate_count`. Persisted peer benchmark
groups can populate claim-time `claim_amount_peer_percentile` when explicit
`service_segment` and provider specialty are available.

## Coverage Matrix

| PRD capability | Current status | Repository evidence | Remaining boundary |
| --- | --- | --- | --- |
| Decision boundary | Implemented | `docs/product/fwa-risk-operations-prd.md`, `docs/project/ml-algorithm-strategy.md`, `apps/api-server/src/routes/agent.rs`, `apps/api-server/tests/knowledge_agent.rs` | Customer governance must still approve production adjudication policy and automatic denial or straight-through rule authority. |
| Inbound claim inbox and canonical trace | Implemented | `apps/api-server/src/routes/inbox.rs`, `apps/api-server/src/routes/claims.rs`, `scripts/demo/tpa_mock_client.py`; field-path response masking verified in `caefdc1` | Customer-specific raw payload adapters and environment-approved PII policy remain external. |
| Core scoring, rules, and review modes | Implemented with baseline and customer-validation boundary | `crates/fwa-scoring`, `crates/fwa-rules`, `apps/api-server/src/routes/ops_rules.rs`, demo seed/smoke, `docs/product/fwa-risk-operations-prd.md`; missing-data reweighting and broader confidence verified in `eccfc06` and `a2e7015`; provider history recency-weighted effective counts verified in `cd3fe54`; clinical compatibility reference-score input path verified in `a63e45f`; clinical compatibility reference worker contract verified in `3aaa58d`; episode utilization scoring inputs verified in `e2b4bf6`; worker artifact-to-scoring context contract verified in `7a4982f`; claims API ingestion of materialized worker contexts verified in `dc70ba2`; persisted scoring-feature-context lookup by claim and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score persisted_worker_feature_context`; persisted provider profile window lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score persisted_provider_profile_rollup`; persisted provider graph signal lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score persisted_provider_graph_signal`; persisted sanctions lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score persisted_provider_sanctions`; persisted peer benchmark lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_peer_benchmark`; persisted clinical compatibility reference lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_clinical_compatibility_reference`; persisted episode rollup lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_episode_rollup`; persisted unbundling comparator lookup and online scoring consumption are covered by `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_unbundling_candidates`; scoring feature context materialization submit/write path verified by `cargo test --locked -p api-server --test ops_datasets scoring_feature_context_materialization` and `cargo test --locked -p worker scoring_feature_context` | Customer-specific approved rule lists, production threshold refs, appeal/override routes, customer-approved ICD-10/CPT or medical-policy reference data, live customer scheduler execution, and live routing-impact evidence are required before production adjudication. |
| Lead, case, QA, medical, and feedback loop | Implemented | `apps/api-server/src/routes/ops_cases.rs`, `apps/api-server/src/routes/ops_medical.rs`, `apps/api-server/src/routes/pilot_loop.rs` | Real reviewer workflow tools and customer operating procedure remain external. |
| Model operations and MLOps pipeline | Implemented with customer validation boundary | `apps/api-server/src/routes/ops_models.rs`, `crates/fwa-ml-runtime`, `apps/worker`, `docs/project/ml-pipeline-runbook.md`; serving manifest metadata caching, PSI actioning, rule hit-rate trending, configured OIG/SAM-compatible sanctions snapshot fetcher, sanctions sync contract plus submit/write path, provider profile rollups plus submit/write path, provider graph signal rollups plus submit/write path, peer benchmark rollups plus submit/write path, worker data pipeline schedule/readiness/execution-report contracts plus readiness/execution audit submit paths, L3 anomaly upgrade readiness, raw sigmoid calibration-status metadata, probability calibration evidence reports, probability calibration governance submit path, and probability-calibration activation gate verified in `1d0fb45`, `b6d56ae`, `b0f00fa`, `d17f3bd`, `429387c`, `d04c3b1`, `37cbd53`, `cargo test --locked -p api-server --test ops_providers sanctions`, `cargo test --locked -p worker sanctions`, `cargo test --locked -p api-server --test ops_providers profile_window`, `cargo test --locked -p worker provider_profile`, `cargo test --locked -p api-server --test ops_providers graph_signal`, `cargo test --locked -p worker provider_graph`, `cargo test --locked -p api-server --test ops_providers peer_benchmark`, `cargo test --locked -p worker peer_benchmark`, `cargo test --locked -p worker worker_data_pipeline`, `cargo test --locked -p api-server --test ops_models probability_calibration`, `cargo test --locked -p api-server --test ops_models activates_candidate_model_after_promotion_gates_pass`, and `cargo test --locked -p worker probability_calibration` | Real labels, live shadow traffic, customer holdout, production drift evidence, official OIG/SAM feed configuration, live customer scheduler execution, calibrated probability serving activation, and validated statistical anomaly scoring. |
| Dataset, feature, and label governance | Implemented with customer validation boundary | `apps/api-server/src/routes/ops_datasets.rs`, `scripts/data/build_public_data_mvp.py`, `docs/project/public-data-mvp.md`; Rust feature-set manifests and online scoring `FeatureValue` payloads include `is_proxy` and `data_source` metadata verified in `25a93a1` and `2f80dbe` | Real customer dataset intake, source data quality, label provenance, and customer-approved feature lineage/source mappings. |
| Knowledge, agent, and AI evidence foundation | Staging proof with partial control-plane contract | `apps/api-server/src/routes/knowledge.rs`, `apps/api-server/src/routes/ops_evidence/mod.rs`, `scripts/ops/build_ai_evidence_foundation.py`; agent registry and investigation grouping contract verified in `fee9ea9`; runtime registry allowlist enforcement verified in `0b8d73a`; agent run cancellation control plane verified in `a7b3835`; deterministic orchestrator/specialist-plan contract verified in `e85d0e5`; crate-level investigation cancellation checkpoints verified in `cbe97a8`; deterministic specialist dispatch/tool mediation contract verified in `51fd3cc`; agent investigation API/output/OpenAPI exposure verified in `2166dc6` | Customer OCR, embedding/vector store, retrieval ranking, masking, retention execution, LLM-backed specialist execution, real external tool-call mediation, and wiring long-running/tool-using agents into the cancellation signal. |
| Analytics scale | Staging proof | `analytics/clickhouse/schema.sql`, `analytics/clickhouse/dashboard_queries.sql`, `scripts/ops/build_analytics_export.py` | Live scheduler credentials, ClickHouse retention/backup/access policy, dashboard hosting. |
| Pilot foundation and staging deployment | Staging proof | `infra/k8s/staging`, `.github/workflows/deploy-staging.yml`, `scripts/ops/build_staging_evidence.py`, `scripts/ops/validate_staging_deployment_package.py` | Customer cluster credentials, secrets, allowlists, observability receiver, restore drill. |
| Production deployment and readiness contract | Implemented as customer-gated contract | `scripts/ops/build_production_deployment_package.py`, `infra/k8s/observability`, `scripts/ops/build_production_readiness_contract.py`; audit-retention dry-run scan contract verified in `3c90fb1` | Live customer cluster apply, real secrets/images/TLS, smoke, alert delivery, restore, rollback, retention archive/destruction execution, and SLO evidence. |
| Web console operations studio | Implemented | `apps/web-console/src/main.rs`, `apps/web-console/src/styles.css`, `scripts/demo/smoke_web_console.mjs` | Customer UAT and role-specific UX refinements. |

## June 2026 Gap Backlog

| Priority | Backlog item | Coverage status |
| --- | --- | --- |
| P0 | Proxy/placeholder labeling for L1 peer benchmark, L5 clinical compatibility, and L3 anomaly baseline | Implemented and verified in `04fbe2c`; labels remain explicit baseline/proxy markers until real peer and clinical compatibility data are connected. |
| P0 | Routing policy lifecycle permission checks | Implemented and verified in `f352721`. |
| P0 | Inbox validation/audit serialization failures must fail loudly | Implemented and verified in `de6e4b5`. |
| P0 | Runtime scorer lock-poison recovery | Implemented and verified in `84741f9`. |
| P1 | Missing-data-aware score reweighting and revised confidence scoring | Implemented and verified in `eccfc06` and `a2e7015`; `cargo test --locked -p fwa-scoring` passed. |
| P1 | Worker-owned OIG/SAM sync and provider 30/90/365 profile windows | OIG/SAM sanctions sync is implemented as a local worker contract in `b6d56ae`; it now includes a configured-endpoint snapshot fetcher that writes the governed `oig_sam_sanctions_snapshot.json` input, and the scheduled worker data pipeline exposes that fetcher as the first daily artifact-only job before `sync-oig-sam-sanctions`. The permission-gated submit/write path persists provider sanctions without changing scoring policy or assigning fraud labels; worker submissions now propagate source/valid/invalid record counts and the API rejects inconsistent governed count evidence, verified in `52b6a68`. Claims scoring can load persisted provider sanctions and trigger sanctions-only provider review when no inline profile is supplied, covered by `cargo test --locked -p api-server --test claims_score persisted_provider_sanctions`; fetch/report/submit behavior remains verified by `cargo test --locked -p api-server --test ops_providers sanctions`, `cargo test --locked -p worker sanctions`, and `cargo test --locked -p worker worker_data_pipeline`. Provider 30/90/365 profile windows are implemented as a local worker contract in `b0f00fa` and now have a permission-gated submit/write path that persists provider profile windows without changing scoring policy or assigning fraud labels; claims scoring can load the latest persisted provider profile window rollup when no inline profile is supplied, covered by `cargo test --locked -p api-server --test claims_score persisted_provider_profile_rollup`; submit/write behavior remains verified by `cargo test --locked -p api-server --test ops_providers profile_window`, `cargo test --locked -p worker provider_profile`, and `cargo check --locked -p worker`. Official feed adapter configuration, production scheduling, and customer claim-history validation remain customer-environment work. |
| P1 | PSI threshold actioning and rule hit-rate runtime trending | Implemented and verified in `d17f3bd`; `cargo test --locked -p worker mlops_monitoring_runtime` and `cargo check --locked -p worker` passed. |
| P1 | Field-path PII/PHI masking for canonical contexts and evidence responses | Implemented and verified in `caefdc1`; `cargo test --locked -p api-server --test inbox normalization`, `cargo test --locked -p api-server routes::pii`, and `cargo check --locked -p api-server` passed. |
| P2 | Billing ring, temporal co-billing, referral entropy, peer percentile, and episode aggregation worker rollups | Provider graph signals for billing-ring membership, temporal co-billing, referral entropy, high-risk neighbors, patient overlap, connected confirmed FWA counts, and network component score are implemented as a local worker contract with a permission-gated submit/write path that persists provider graph signals without changing scoring policy, assigning fraud labels, or opening cases; complete persisted graph signals can be consumed by `/api/v1/claims/score` when no inline `provider_relationships` payload is supplied. Verified by `cargo test --locked -p api-server --test ops_providers graph_signal`, `cargo test --locked -p api-server --test claims_score persisted_provider_graph_signal`, and `cargo test --locked -p worker provider_graph`. Peer percentile benchmarks are implemented as a local worker contract in `242859a` and now have a permission-gated submit/write path that persists peer benchmark groups without scoring claims, assigning fraud labels, or changing routing policy; claims scoring can load the latest persisted peer benchmark group by provider specialty, provider region, and explicit service segment when no inline or materialized peer context is supplied, covered by `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_peer_benchmark`; submit/write behavior remains verified by `cargo test --locked -p api-server --test ops_providers peer_benchmark` and `cargo test --locked -p worker peer_benchmark`. Episode aggregation is implemented as a local worker contract in `0cb2a23` and now has a permission-gated submit/write path that persists member-provider episode rollups without changing scoring policy, assigning fraud labels, opening cases, or denying claims; claims scoring can load the latest persisted episode rollup by member/provider when no inline or materialized episode context is supplied, covered by `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_episode_rollup`; submit/write behavior remains verified by `cargo test --locked -p api-server --test ops_providers episode` and `cargo test --locked -p worker episode`. Production scheduling, customer claim history validation, and scheme-specific code comparators remain open. |
| P2 | Agent registry, investigations entity, PHI field allowlists, and populated `phi_fields_accessed` | Implemented as the deterministic-agent control-plane contract in `fee9ea9` and `0b8d73a`: additive `agent_registry` and `investigations` schema contract, stable investigation ids for agent audit events, exposed agent identity/investigation fields in run logs, non-empty PHI field audit records, and active registry capability/PHI allowlist enforcement before `knowledge.search_similar` execution. Multi-agent runtime orchestration remains P3. |
| P2 | Raw sigmoid probability calibration visibility | Implemented and verified in `d04c3b1`: Rust artifact and serving-manifest scorers emit `probability_calibration_status = uncalibrated_raw_sigmoid`. Actual probability calibration against customer labels remains open. |
| P2 | Probability calibration evidence report | Implemented as a worker report contract in `37cbd53`: `build-probability-calibration-report` computes ECE, Brier score, calibration bins, and review-task artifacts from labeled holdout predictions. Probability calibration reports now have a model-governance submit path that persists calibration evidence and records audit lineage without activating calibrated serving, changing thresholds, or assigning labels. The latest same-version passing calibration report is also consumed by model promotion gates, so model activation is blocked when calibration evidence is missing or failing; verified by `cargo test --locked -p api-server --test ops_models probability_calibration`, `cargo test --locked -p api-server --test ops_models activates_candidate_model_after_promotion_gates_pass`, and `cargo test --locked -p worker probability_calibration`. Fitting/activating a calibrated probability model remains customer-data work. |
| P2 | Feature proxy/source metadata | Implemented for Rust training/governance manifests and online scoring responses in `25a93a1` and `2f80dbe`: feature-set columns and `FeatureValue` payloads now carry `is_proxy` and `data_source`, with OpenAPI and claims-score response tests covering the API contract. Customer-approved source lineage mappings remain external. |
| P2 | Clinical compatibility feature input path and reference worker contract | Implemented in `a63e45f` and `3aaa58d`: `fwa-features` accepts `ClinicalCompatibilityFeatureContext`, and the worker can build a governed clinical compatibility reference report with policy authority refs, evidence refs, low-score review tasks, and records marked for `worker.icd_cpt_compatibility_reference:*`. Clinical compatibility references now have a permission-gated submit/write path that persists customer policy reference rows without scoring claims, assigning labels, denying claims, or replacing medical review. `/api/v1/claims/score` can load a matching persisted clinical compatibility reference by diagnosis prefix and procedure code when no inline or materialized clinical context is supplied, preserving non-proxy `data_source` and evidence refs in the scoring response; verified by `cargo test --locked -p api-server --test ops_datasets clinical_compatibility`, `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_clinical_compatibility_reference`, and `cargo test --locked -p worker clinical_compatibility`. Customer-approved reference data, live scheduler execution, and customer-data validation remain open. |
| P2 | Unbundling comparator worker contract | Implemented in `16449c8`: `build-unbundling-comparator` joins customer-governed bundled/component code rules with episode procedure-code snapshots and emits evidence-backed medical-review candidates. Unbundling candidates now have a permission-gated submit/write path that persists comparator candidates without scoring claims, assigning fraud labels, denying claims, opening cases, or replacing medical review. `/api/v1/claims/score` can load the latest persisted comparator candidates by member/provider and populate `unbundling_candidate_count` when no inline or materialized episode context supplies it; verified by `cargo test --locked -p api-server --test ops_datasets unbundling`, `cargo test --locked -p api-server --test claims_score scores_full_payload_with_persisted_unbundling_candidates`, and `cargo test --locked -p worker unbundling`. Customer-approved unbundling rule packs, production scheduling, and customer-data validation remain open. |
| P2 | Episode utilization scoring features | Implemented in `e2b4bf6` and wired through the claims API in `dc70ba2`: `fwa-features` accepts worker-owned member-provider revisit counts, duplicate-claim similarity, procedure-frequency peer percentile, and unbundling candidate counts, and `fwa-scoring` consumes them in L5 with bounded risk contribution. Episode rollups and unbundling comparator artifacts now have permission-gated submit/write paths; `/api/v1/claims/score` can also load a matching persisted episode rollup and latest unbundling comparator candidates by member/provider when no inline or materialized episode context is supplied. Production scheduling and customer-data validation remain open. |
| P2 | Worker artifact-to-scoring context materialization | Implemented in `7a4982f` and API-ingested in `dc70ba2`: `build-scoring-feature-contexts` maps episode rollups, peer benchmarks, clinical compatibility records, and unbundling candidates into claim-level `PeerFeatureContext`, `ClinicalCompatibilityFeatureContext`, and `EpisodeUtilizationFeatureContext` payloads; `/api/v1/claims/score` accepts inline payloads and now also loads the latest persisted materialized context by claim when the request does not supply one, preserving the resulting `feature_values` and evidence trace. The productionization follow-up adds `submit-scoring-feature-contexts` and `/api/v1/ops/scoring-feature-context-materializations` so worker materializations can be permission-gated, audited, persisted, and consumed before scoring. Production scheduling and customer-data validation remain open. |
| P2 | Worker data pipeline schedule, readiness, and execution evidence contracts | Implemented as worker artifacts plus audit write paths: `build-worker-data-pipeline-plan` defines daily/monthly cadence, artifact roots, command order, dependency ordering, API submit endpoints, required permission scopes, idempotency key shape, readiness gates, and a no-adjudication side-effect boundary across sanctions, provider profile, graph signals, peer benchmarks, episodes, clinical references, unbundling, scoring context materialization, and probability calibration. OIG/SAM sanctions now start with an artifact-only `oig_sam_sanctions_snapshot_fetch` job, then `oig_sam_sanctions_sync` depends on that governed snapshot before submitting sanctions upsert evidence. `build-worker-data-pipeline-readiness-input-template` generates the customer-fillable readiness evidence input from the plan, including build commands, source inputs, dependencies, required permission scopes, planned artifact URIs, planned API paths, default blocked approval state, minimum row-count placeholders, and evidence refs to fill. `build-worker-data-pipeline-readiness-report` validates customer-approved artifact URIs, minimum row counts, data-quality status, external OIG/SAM fetch configuration, evidence refs, and per-job permission scope evidence before scheduled writes; `/api/v1/ops/worker-data-pipeline-readiness` persists that prerequisite evidence and records audit lineage without fetching external data or submitting artifacts. `fetch-oig-sam-sanctions-snapshot` can fetch customer-configured OIG/SAM-compatible JSON endpoints into the governed sanctions snapshot artifact shape when the customer environment supplies approved URLs. `build-worker-data-pipeline-run-status-template` generates the customer scheduler input contract with an explicit readiness report URI, build commands, source inputs, dependency lists, artifact-only markers, required permission scopes, pending job statuses, and planned artifact/API targets. `build-worker-data-pipeline-execution-report` converts a customer scheduler run-status artifact into per-job execution status, readiness-gate status, readiness evidence refs, dependency blockers, required permission scopes, planned API paths, and operations review tasks; it treats artifact-only jobs as complete when a successful artifact URI is reported without requiring API submission, and marks downstream jobs `dependency_not_completed` when their planned dependencies are missing or incomplete. `/api/v1/ops/worker-data-pipeline-executions` persists that scheduler evidence and records audit lineage without scoring claims, assigning labels, denying claims, activating models, or changing routing policy. The API write paths now also validate dependency-blocked execution evidence, pending/failed job counts, per-job readiness status counts, blocked-job blocker details, and worker review-task permission scopes before persisting scheduler evidence, verified in `2864572`, `0c0c117`, `165d400`, and `de409d7`; worker-generated readiness/execution review tasks carry the same planned `api_path` evidence as of `82c0bef`. Missing or blocked readiness evidence now remains visible in execution evidence instead of silently bypassing the customer-data readiness gate. Live scheduler deployment, official feed credentials/configuration, and customer-data validation remain open. |
| P3 | L3 anomaly statistical upgrade readiness | Implemented as a worker report contract in `429387c`: `build-anomaly-upgrade-readiness` checks `confirmed_fwa_label_count >= 500`, flags low 30-day recall, and emits review-task artifacts without changing scoring. Validated IQR/MAD or ensemble replacement remains open. |
| P3 | Audit retention scan contract | Implemented as a dry-run worker report in `3c90fb1`: `build-audit-retention-scan` computes six-year cutoff archive candidates, destruction-review candidates, legal-hold blocks, and source-record review tasks without deleting records. Customer-environment partitioning, archive storage, legal-hold reconciliation writes, and approved destruction execution remain open. |
| P3 | Agent run cancellation control plane and execution checkpoints | Implemented in `a7b3835` and `cbe97a8`: `POST /api/v1/ops/agent-runs/{agent_run_id}/cancel` marks queued/running runs as `cancelled`, requires `agent_run:{id}` evidence, rejects terminal-state recancellation, and emits `agent.run.cancelled` governance audit events; `fwa-agent` exposes cancellation signals and deterministic investigation checkpoints. Wiring long-running/tool-using specialist agents into that signal remains open. |
| P3 | Deterministic specialist-orchestrator and dispatch contract | Implemented in `e85d0e5`, `51fd3cc`, and `2166dc6`: `fwa-agent` now exposes `InvestigationOrchestrator`, deterministic specialist tasks for intake triage/evidence review/network analysis, PHI field scopes, assistive-only boundary assertions, specialist execution records, and mediated tool-call contracts marked `contract_only_not_executed`; `/api/v1/agent/cases/investigate` returns and persists `specialist_executions`, and the OpenAPI contract describes those records. LLM-backed specialist execution and real external tool mediation remain open. |
| P3 | Six-year audit retention execution and statistical L3 anomaly scorer replacement | Medium-term production hardening. |

## Practical Reading

For progress accounting, count repository-owned PRD work as complete for demo
and pilot-contract purposes except for customer data and customer environment
execution. Do not claim production model effectiveness, ROI accuracy, or live
drift safety until customer labels, holdout validation, and shadow traffic exist.
