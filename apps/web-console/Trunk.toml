[build]
target = "index.html"
dist = "dist"
public_url = "/"
filehash = true
minify = "never"

[serve]
addresses = ["127.0.0.1"]
port = 5173

[[proxy]]
backend = "http://127.0.0.1:8080/api/"
rewrite = "/api/"
