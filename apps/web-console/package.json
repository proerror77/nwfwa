{
  "name": "fwa-web-console",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NO_COLOR=false trunk serve",
    "build": "NO_COLOR=false trunk build --release --locked",
    "lint": "cargo fmt -- --check && cargo check --locked --target wasm32-unknown-unknown",
    "smoke:build": "node ../../scripts/demo/smoke_web_console.mjs",
    "test": "cargo check --locked --target wasm32-unknown-unknown"
  }
}
